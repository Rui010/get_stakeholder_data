以下のHTMLまたは表形式のテキストデータを、次のJSONフォーマットで整形してください。

- 出力は必ずJSON形式のみで行ってください（解説・補足は禁止）
- JSONのトップレベルキーは `大株主の状況` とし、以下の構造を持ちます：
{{
  "大株主の状況": {{
    "date": "YYYY年MM月DD日現在",
    "data": [
      {{
        "氏名又は名称": string,
        "住所": string,
        "所有株式数(千株)": string,
        "所有割合(％)": string
      }}
    ]
  }}
}}

以下が入力データです：

{xml_data}